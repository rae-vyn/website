---
import Headerjunk from "@components/header-junk.astro";
import Navbar from "@components/navbar.astro";
const { frontmatter } = Astro.props;
---
<html lang="en">
    <head>
        <Headerjunk title={frontmatter.title}/>
        <Navbar />
        <title>{frontmatter.title}</title>
    </head>
    <body>
        <h1>{frontmatter.title}</h1>
        <hr />
        <slot />
    </body>
</html>